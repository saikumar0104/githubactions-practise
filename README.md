A workflow contains 7 main components:

1.name --> WorkFlow name

2.on (triggers) --> push, workload_dispatch

3.jobs --> A workflow is made of one or many jobs

  - runs-on --> Which machine the job runs on

  - steps --> Tasks inside a job

  - uses --> Use a predefined action

  - run --> Execute shell commands
